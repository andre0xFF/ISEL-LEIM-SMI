<project
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
>

    <modelVersion>4.0.0</modelVersion>

    <groupId>smi.ws.java.rest.extra.bookstore</groupId>
	<artifactId>smi.ws.java.rest.extra.bookstore</artifactId>
	<version>1.0</version>

    <packaging>war</packaging>

	<name>Java WS REST Extra - BookStore - Server (Tomcat)</name>

    <description
    >Web Services REST Examples in Java - BookStore deploy on Apache Tomcat</description>

    <properties>
		<java.version>11</java.version>

		<JarFileName>ServiceBookStoreREST-Tomcat</JarFileName>

        <war.deploy.name>${JarFileName}</war.deploy.name>

		<junit.version>4.13.2</junit.version>
        <commons-logging.version>1.3.0</commons-logging.version>
		<jersey.version>1.18.3</jersey.version>
		<javax.servlet-api.version>3.1.0</javax.servlet-api.version>
    </properties>

	<dependencyManagement>
        <dependencies>
            <!-- Junit - Begin -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <!-- Junit - End -->

            <!-- Commons Logging - Begin -->
            <dependency>
                <groupId>commons-logging</groupId>
                <artifactId>commons-logging</artifactId>
                <version>${commons-logging.version}</version>
            </dependency>
            <!-- Commons Logging - End -->

			<!-- Jersey - Begin -->
            <dependency>
                <groupId>com.sun.jersey</groupId>
                <artifactId>jersey-servlet</artifactId>
                <version>${jersey.version}</version>
            </dependency>

            <dependency>
                <groupId>com.sun.jersey</groupId>
                <artifactId>jersey-json</artifactId>
                <version>${jersey.version}</version>
            </dependency>
            <!-- Jersey - End -->

			<!-- Servlet-API (for Servlet context) - Begin -->
			<dependency>
				<groupId>javax.servlet</groupId>
				<artifactId>javax.servlet-api</artifactId>
				<version>${javax.servlet-api.version}</version>
				<type>jar</type>
			</dependency>
			<!-- Servlet-API - End -->
		</dependencies>
	</dependencyManagement>

    <dependencies>
        <!-- JUnit - Begin -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>
        <!-- JUnit - End -->

        <!-- Commons Logging - Begin -->
        <dependency>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
        </dependency>
        <!-- Commons Logging - End -->

        <!-- Jersey - Begin -->
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-servlet</artifactId>
        </dependency>

        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-json</artifactId>
        </dependency>
        <!-- Jersey - End -->

        <!-- Servlet-API (for Servlet context) - Begin -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <type>jar</type>
        </dependency>
        <!-- Servlet-API - End -->
    </dependencies>

    <build>

        <plugins>
            <!-- Compile the code using a specific java version (not maven defaults) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>

                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>

            <!-- Generate the WAR file -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>

                <configuration>
                    <warName>${war.deploy.name}</warName>

                    <!-- web.xml is not mandatory since JavaEE 5 -->
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
